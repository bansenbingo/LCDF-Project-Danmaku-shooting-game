---
alwaysApply: true
scene: git_message
---

1.提交代码的时候，提交信息要符合 git commit 规范，包括提交信息的格式、内容、长度等。
2.使用英文编写提交信息.
3.提交信息前加标号，标号的含义为某一类操作，例如对markdown的编辑为0号，修改工程文件中不同模块的源代码分别为1、2、3...号操作，如果有删除操作，标号为-1，如果创建新文件，标号依次+1，如果这些创建的文件服务于同一模块，则其标号相同。每次修改一个文件时，在文件后加. ，例如创建2号文件后，第一次修改就是2.1，第二次为2.2，以此类推。
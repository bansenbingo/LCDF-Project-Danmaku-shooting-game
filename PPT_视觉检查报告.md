# 弹幕射击游戏 PPT 视觉检查报告

## 概述
- **文件**: `弹幕射击游戏_实验介绍.pptx`
- **幻灯片数量**: 12 张
- **主题**: 深色背景 (#0B1120)，青色强调色 (#06B6D4)
- **检查方法**: soffice 转 PDF → pdftoppm 转图片 + python-pptx 结构分析 + PIL 像素级分析

---

## 一、通用问题（影响全部或多张幻灯片）

### 1. 幻灯片编号对比度不足 -- 所有幻灯片
**严重程度**: 中等

幻灯片页码（如 "2 / 12"）使用 `RGB(100, 116, 139)`（灰蓝色）绘制在深色背景 `RGB(11, 17, 32)` 上，对比度仅 **4.0:1**，低于 WCAG AA 标准（4.5:1）。

**建议**: 将页码颜色从 `#64748B` 改为 `#94A3B8` 或更亮的灰色，使对比度达到 4.5:1 以上。

### 2. 中英双语标题重叠 -- 所有幻灯片
**严重程度**: 提示

每张幻灯片的中文标题（如"实验背景"）和英文副标题（如"BACKGROUND"）之间存在约 20% 的垂直重叠区域（中文标题 bottom=0.83"，英文副标题 top=0.72"）。

**分析**: 这是有意的双语标题设计，英文副标题紧贴中文标题下方。4mm 左右的重叠从视觉上呈现紧凑的层次效果，如设计意图如此则无需修改。

### 3. 标题栏覆盖顶边 -- 所有幻灯片
**严重程度**: 提示

标题栏背景矩形 ("Shape 0") 从 `(0.000", 0.000")` 开始填充，覆盖整个顶边。顶部 15px 区域有 60-80% 的非背景像素。这是暗色渐变标题栏的设计效果，并非问题。

### 4. 背景噪声 -- 所有幻灯片
**严重程度**: 低

JPEG 导出的背景区域有约 9-10 的标准差噪声（理想值应更低）。这是 soffice→PDF→JPEG 转换过程中的压缩产生的，原始 PPTX 中不存在此问题。

---

## 二、逐张幻灯片详细检查

---

### 幻灯片 1：封面 (AEROPLANE DANMAKU SHOOTING)

**结论**: 基本正常，有轻微问题。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | 无 |
| 文本溢出 | 无 |
| 占位符文本 | 无 |
| 边缘裁切 | 轻微 |

**发现的问题**:

1. **Shape 81, Shape 161 超出右边界**
   - 两个装饰形状右侧延伸至 10.060"（幻灯片宽度 10.000"），超出 0.06"（约 1.5mm）。
   - **严重程度**: 低。实际渲染时可能被裁切或不可见，但可能在导出/打印时产生细微异常。

2. **底部科技说明行对比度偏低**
   - "Vivado 2025.1  |  XC7K160TFFG676-1  |  640×480@60Hz VGA" 使用 `RGB(100,116,139)`，对比度为 4.0:1。
   - **严重程度**: 中等。投影环境下可能难以辨认。

---

### 幻灯片 2：实验背景 (BACKGROUND)

**结论**: 有需要关注的对比度问题。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | 有意设计（标题 + 序号徽章） |
| 文本溢出 | 无（多行文本，高度足够） |
| 占位符文本 | 无 |
| 边缘裁切 | 正常 |

**发现的问题**:

1. **序号徽章颜色极暗 -- 重点问题**
   - "01", "02", "03" 三个序号徽章使用 `RGB(14, 116, 144)`（深青色），在 `#0B1120` 背景上对比度仅为 **3.5:1**。
   - 这是 12 张幻灯片中最严重的对比度问题。
   - **严重程度**: 高。投影时极可能看不清序号数字。
   - **建议**: 将颜色改为 `#06B6D4`（标准青色强调色）或 `#22D3EE`，使对比度达到 7:1 以上。

2. **内容描述文本估计过宽**
   - 三个内容区域的文本宽度估计（644pt, 661pt, 625pt）远超文本框宽度（173pt），但由于文本框高度充足（2.50"），多行文本已正确换行。实际渲染应该正常。

---

### 幻灯片 3：硬件平台 (HARDWARE PLATFORM)

**结论**: 有文本溢出和中英标签重叠的问题。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | 有（中英标签） |
| 文本溢出 | 有（输入设备描述） |
| 占位符文本 | 无 |
| 边缘裁切 | 正常 |

**发现的问题**:

1. **中英文硬件标签重叠**
   - 右侧框图区域中 7 组中英文标签对（如 "PS/2" ↔ "键盘"、"VGA" ↔ "640×480"、"BTN" ↔ "矩阵按键" 等）各存在约 8% 的轻微重叠。
   - 中文标签紧接英文标签右侧，无缝布局。
   - **严重程度**: 低。视觉上可能很紧凑但应可读。

2. **输入设备描述可能文字溢出**
   - "PS/2 键盘 + 5 个板载按键 + 16 路拨码开关"（11pt）在 3.40" 宽的文本框中，估计宽度 260pt vs 可用 245pt，溢出约 6%。
   - **严重程度**: 中等。文字可能被截断或产生意外换行。

3. **标签对比度不足**
   - "键盘"、"640×480"、"数码管"、"指示灯"、"矩阵按键"、"拨码开关" 等中文标签使用 `RGB(100,116,139)`，对比度 4.0:1。
   - **严重程度**: 中等。

4. **暗色内容像素比例高** (65.8%)
   - 图片分析显示 65.8% 的内容像素灰度值 < 50，表明大量元素使用暗色（深色连线、框图边框等）。在投影环境下这些细节可能难以辨识。
   - **严重程度**: 中等。

---

### 幻灯片 4：设计思路 (DESIGN APPROACH)

**结论**: 存在文字溢出风险，但可能已被自动换行处理。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | 无（标题除外） |
| 文本溢出 | 有风险（4处） |
| 占位符文本 | 无 |
| 边缘裁切 | 正常 |

**发现的问题**:

1. **4 条设计要点描述文字可能溢出**
   - 每条描述都是长句（如 "VGA 渲染使用纯组合逻辑...避免时序耦合"），10.5pt 字体放在 8.00" 宽、0.45" 高的文本框中：
     - 第 1 条: 750pt 估计 / 576pt 可用（超出 30%）
     - 第 2 条: 629pt 估计 / 576pt 可用（超出 9%）
     - 第 3 条: 639pt 估计 / 576pt 可用（超出 11%）
     - 第 4 条: 664pt 估计 / 576pt 可用（超出 15%）
   - 文本框高度仅 0.45"，仅能容纳 1-2 行文字。
   - **严重程度**: 中高。如果文本框未设置自动换行/自动调整，文字将被截断。**建议检查**这些文本框的 `autoSize` 属性。

---

### 幻灯片 5：系统架构 (SYSTEM ARCHITECTURE)

**结论**: 模块名称和描述标签存在上下重叠。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | 有（模块标签） |
| 文本溢出 | 无 |
| 占位符文本 | 无 |
| 边缘裁切 | 轻微（右侧模块） |

**发现的问题**:

1. **渲染模块名称与描述标签上下重叠**
   - 6 组模块标签对（如 "vgac.v" ↔ "640×480 / 时序"、"player_render" ↔ "玩家 / 飞机"）各有 17% 重叠。每个模块名称框 (y=2.85-3.15) 与下方的描述框 (y=3.10-3.55) 有 0.05" 重叠。
   - 这是架构图中的紧凑排列，描述直接位于模块下方。
   - **严重程度**: 低。紧凑但属于有意设计的框架图样式。

2. **"menu_render" 模块靠近右边界**
   - 位置 right=9.90"，仅距右边界 0.10"。
   - **严重程度**: 低。边距偏紧但未出界。

3. **暗色内容比例高** (62.5%)
   - 同幻灯片 3，连接线和框图边框在投影时可能偏暗。
   - **严重程度**: 中等。

---

### 幻灯片 6：核心模块详解 (CORE MODULES)

**结论**: 存在文字溢出和对比度问题。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | 无（标题除外） |
| 文本溢出 | 有（3处） |
| 占位符文本 | 无 |
| 边缘裁切 | 正常 |

**发现的问题**:

1. **3 个模块描述文字可能溢出**
   - PS/2 接收器模块: 487pt / 432pt（超出 13%）
   - 玩家渲染模块: 515pt / 432pt（超出 19%）
   - 敌机渲染模块: 463pt / 432pt（超出 7%）
   - 文本框宽度 6.00"、高度 0.45"，10pt 字体。
   - **严重程度**: 中等。如同幻灯片 4，需确认 autoSize/wrap 设置。

2. **模块子标题对比度不足**
   - "PS/2 接收器"、"玩家渲染"、"敌机渲染"、"子弹渲染"、"障碍物渲染"、"HUD 界面" 均使用 `RGB(100,116,139)`，对比度 4.0:1。
   - **严重程度**: 中等。

---

### 幻灯片 7：键盘输入系统 (PS/2 KEYBOARD INPUT)

**结论**: **存在严重布局重叠问题**，需要重点修复。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | **严重** |
| 文本溢出 | 无 |
| 占位符文本 | 无 |
| 边缘裁切 | 正常 |

**发现的问题**:

1. **按键状态管理区域严重重叠 -- 关键问题**
   - 右侧"按键状态管理"栏目中，**多组文本框完全或接近完全重叠**：
     - "K" 按键说明 → 与 "按键状态管理" 标题重叠 **43%**
     - "1/2/3/4" → 与详情文本重叠 **100%**（完全被覆盖）
     - "选择难度" → 与详情文本重叠 **94%**
     - "BTN[3:0] + BTNX4" → 与详情文本重叠 **100%**
     - "板载按键备份" → 与详情文本重叠 **94%**
   - 详情文本框占据 `(6.30, 3.90) - (9.60, 4.90)`，而 5 组按键说明框都在此范围内。
   - **严重程度**: **严重**。多行文字被互相覆盖，在投影/显示时内容完全无法阅读。**需要立即修复**：检查 `z-order` 或调整各组文本的 Y 坐标位置。

2. **暗色内容比例高** (54.6%)
   - **严重程度**: 中等。

---

### 幻灯片 8：VGA 渲染管线 (RENDERING PIPELINE)

**结论**: 存在中等程度的重叠问题。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | 有（箭头 + 右侧列表） |
| 文本溢出 | 无 |
| 占位符文本 | 无 |
| 边缘裁切 | 轻微 |

**发现的问题**:

1. **优先级列表与 Frame Tick 说明重叠 -- 重点问题**
   - 右侧列中的 "5. Enemies (敌机 UFO)" → 与 "Frame Tick 生成" 重叠 **73%**
   - "6. Obstacles (障碍物)" → 与 "vs 下降沿检测 → 60Hz..." 重叠 **100%**
   - "7. Menu / Background" → 与相同区域重叠 **33%**
   - **严重程度**: 中高。优先级列表的元素和 Frame Tick 描述框在相近的 Y 范围内，内容互相遮挡。

2. **箭头符号与模块框重叠**
   - 6 个 ▶ 箭头分别与相邻的模块渲染框有约 25% 重叠。这是流程图中箭头连接相邻框的设计效果。
   - **严重程度**: 低。属于设计意图。

3. **右边界内容接近边缘**
   - "vgac / vga_data" 区域 right=9.95"，仅距边界 0.05"。
   - **严重程度**: 低。

4. **暗色内容比例高** (60.7%)
   - **严重程度**: 中等。

---

### 幻灯片 9：游戏核心机制 (GAME MECHANICS)

**结论**: 基本正常，有轻微重叠。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | 轻微（页码与内容） |
| 文本溢出 | 无 |
| 占位符文本 | 无 |
| 边缘裁切 | 正常 |

**发现的问题**:

1. **页码 "9/12" 与碰撞检测内容重叠**
   - 页码文本框与底部的"AABB 碰撞检测/玩家子弹 vs 敌机..."内容区域存在重叠。如果页码显示在内容上方，可能遮挡文字。
   - **严重程度**: 低。取决于页码的 z-order 位置。

---

### 幻灯片 10：显示与计分系统 (DISPLAY & SCORING)

**结论**: 存在元素重叠问题。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | 有（计分规则区域） |
| 文本溢出 | 无 |
| 占位符文本 | 无 |
| 边缘裁切 | 正常 |

**发现的问题**:

1. **SCORE / SEGMODE 标签完全被覆盖 -- 重点问题**
   - "SCORE" (y=4.12-4.37) → 与溢出说明文本 (y=4.08-4.60) 重叠 **100%**
   - "SEGMODE_SCORE" (y=4.38-4.60) → 与溢出说明文本重叠 **100%**
   - **严重程度**: 中高。这两个标签文字被完全遮挡，无法阅读。

2. **菜单模拟区域与 7 段数码管说明重叠**
   - AEROPLANE DANMAKU SHOOTING 菜单区域与 "7段数码管 (循环切换 K键)" 有 12% 重叠。
   - **严重程度**: 低。

3. **对比度问题**
   - "PRESS START" 提示文字、SEGMODE_KILLS/LIVES/SCORE 标签使用 `RGB(100,116,139)`，对比度 4.0:1。
   - **严重程度**: 中等。

---

### 幻灯片 11：实现亮点 (IMPLEMENTATION HIGHLIGHTS)

**结论**: 检查通过，无重大问题。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | 无（标题除外） |
| 文本溢出 | 无 |
| 占位符文本 | 无 |
| 边缘裁切 | 正常 |

**仅有**页码对比度问题（4.0:1），这在所有幻灯片中都存在。

---

### 幻灯片 12：总结与展望 (SUMMARY & FUTURE WORK)

**结论**: 基本正常。

| 检查项 | 结果 |
|--------|------|
| 元素重叠 | 无（标题除外） |
| 文本溢出 | 无 |
| 占位符文本 | 无 |
| 边缘裁切 | 正常 |

**发现的问题**:

1. **标题栏延伸至底部边界**
   - 背景矩形 "Shape 1" bottom=5.625"（正好等于幻灯片高度），底部 15px 有 66.7% 非背景像素。
   - 底边有不透明的装饰条。
   - **严重程度**: 提示。如果这是底部装饰条，则属于预期设计。

2. **页脚对比度不足**
   - "数字逻辑设计课程 · Xilinx K7 FPGA · Vivado 2025.1 · Verilog HDL" 使用 `RGB(100,116,139)`，对比度 4.0:1。
   - **严重程度**: 中等。

---

## 三、问题汇总与优先级

### 严重问题（需立即修复）

| # | 幻灯片 | 问题 | 描述 |
|---|--------|------|------|
| 1 | **7** | 按键状态管理区域 100% 重叠 | 5 组文本被 "make → set key=1..." 详情框完全覆盖，内容不可读 |
| 2 | **2** | 序号徽章对比度 3.5:1 | "01/02/03" 使用 RGB(14,116,144)，投影时几乎不可见 |

### 中高优先级问题

| # | 幻灯片 | 问题 | 描述 |
|---|--------|------|------|
| 3 | **10** | SCORE/SEGMODE 标签 100% 被覆盖 | 两个标签被溢出说明文本完全遮挡 |
| 4 | **8** | 优先级列表与 Frame Tick 重叠 73-100% | "5. Enemies" 等行被 "Frame Tick 生成" / "vs 下降沿检测" 覆盖 |
| 5 | **4** | 4 条设计要点文字可能溢出（30%/9%/11%/15%） | 需确认文本框 autoSize 属性 |

### 中等优先级问题

| # | 幻灯片 | 问题 | 描述 |
|---|--------|------|------|
| 6 | **全部** | 页码对比度 4.0:1 | 低于 WCAG AA 4.5:1 标准 |
| 7 | **3/5/7/8/9** | 暗色内容 >50% | 连线、框图边框在投影时可能看不清 |
| 8 | **6** | 3 个模块描述可能溢出 | 需确认文本框设置 |
| 9 | **3** | 输入设备描述溢出 6% | "PS/2 键盘 + 5 个板载按键 + 16 路拨码开关" |

### 低优先级问题

| # | 幻灯片 | 问题 | 描述 |
|---|--------|------|------|
| 10 | **1** | Shape 81/161 超出右边界 0.06" | 装饰元素轻微超界 |
| 11 | **3** | 中英文标签紧凑排列 | 各 8% 重叠，视觉可接受 |
| 12 | **5** | 渲染模块名与描述上下紧贴 | 框架图设计风格 |
| 13 | **8** | 箭头 vs 模块框 | 流程图设计风格 |
| 14 | **10** | 菜单区域 vs 数码管说明 | 12% 重叠 |

---

## 四、检查结论

12 张幻灯片中：
- **2 张存在严重问题**：幻灯片 7（布局重叠）和幻灯片 2（对比度）
- **3 张存在中高优先级问题**：幻灯片 10、8、4
- **3 张存在中等优先级问题**：幻灯片 3、5、6
- **4 张基本正常**：幻灯片 1、9、11、12（仅全局页码对比度问题）

**建议优先修复幻灯片 7 的按键状态管理布局**，这是整个 PPT 中最严重的问题 -- 多组文字被完全覆盖导致内容不可读。
